# Stellar Network Configuration
NEXT_PUBLIC_STELLAR_NETWORK=testnet

# Contract ID (optional - uses default if not provided)
NEXT_PUBLIC_CONTRACT_ID=your_contract_id

# Google OAuth Client ID
# Get from: https://console.cloud.google.com/apis/credentials
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id_here

# Sponsor Secret Key - CRITICAL: KEEP SECRET - Server-side only
# This is the private key of the developer/sponsor that will pay transaction fees for gasless transactions
# For testnet, generate at: https://laboratory.stellar.org/#account-creator
# SECURITY: Never commit this to version control or expose to the client
SPONSOR_SECRET_KEY=your_sponsor_secret_key_here
